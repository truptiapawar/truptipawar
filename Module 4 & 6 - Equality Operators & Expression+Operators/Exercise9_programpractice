//Create a method to determine whether person is minor or major using person's age. Age should be received as parameter
function determinemajororminor(a)
{
  if (a<18)
  {console.log("Person is Minor");}
  else 
  {console.log("Person is Major");}
}
determinemajororminor(20);